# Лабораторная 11.20 — Чтение WAV IQ RTL-SDR, демодуляция OTA BPSK и измерение BER

## Цель

Офлайн обработать запись WAV IQ, сделанную RTL-SDR в роли пассивного RF-монитора, демодулировать BPSK-пакет, захваченный с антенны TX ZynqSDR, и сообщить BER и EVM относительно известной эталонной битовой последовательности курса.

Эта работа — офлайн-анализ, парный к работам с живым захватом (11.21–11.22). Она принимает путь к файлу WAV (плюс необязательный манифест YAML) и создаёт самодостаточный пакет доказательств BER/EVM без какого-либо железа.

## Контекст

Подход с внешним монитором RTL-SDR принят потому, что:

- тракт PL RX у ZynqSDR по-прежнему стоит на `rx_valid_count = 0`;
- RTL-SDR может независимо принимать сигнал TX ZynqSDR по воздуху;
- запись WAV, сделанную в Lab 11.21 или 11.22, можно воспроизводить офлайн любое число раз с разными параметрами демодулятора.

## Файлы

| Путь | Назначение |
|---|---|
| `blocks/block_11_integrated_sdr_project/python/lab_11_20_read_rtl_wav_ota_bpsk_ber.py` | загрузить WAV, нормализовать IQ, обнаружить кадр BPSK, вычислить BER/EVM |

## Запуск

```bash
python blocks/block_11_integrated_sdr_project/python/\
lab_11_20_read_rtl_wav_ota_bpsk_ber.py \
  --wav-path datasets/lab11_21_rtl_monitor/capture_live_20260623.wav \
  --manifest-path datasets/lab11_21_rtl_monitor/manifest_live_20260623.yaml \
  --run-tag rtl_ota_bpsk_ber_live
```

## Цепочка обработки

```
файл WAV (int16, чередующиеся I/Q)
  → нормализация в комплексный float
  → перенос частоты в основную полосу (если в манифесте есть смещение центра)
  → согласованный фильтр (RRC, 16 sps)
  → восстановление тайминга (Гарднер или пик по порогу)
  → дифференциальное или когерентное решение BPSK
  → поиск кадра (корреляция с преамбулой)
  → BER и EVM относительно эталонных бит
  → графики и метрики JSON
```

## Ожидаемый формат WAV

WAV должен быть 16-битным стерео (левый = I, правый = Q) либо 16-битным моно с чередующимися I/Q, как выдаёт помощник захвата RTL-SDR (Lab 11.21 / 11.22).

```text
sample_rate = частота из заголовка WAV (например, 3 840 000 или 2 400 000)
samples     = 16-битные знаковые целые
layout      = I[0] Q[0] I[1] Q[1] ...
```

## Ожидаемые результаты

| Файл | Содержимое |
|---|---|
| `docs/assets/lab1120_<tag>_spectrum.png` | спектр RX в основной полосе |
| `docs/assets/lab1120_<tag>_constellation.png` | созвездие символов |
| `docs/assets/lab1120_<tag>_ber_metrics.json` | BER, EVM, число отсчётов, смещение тайминга |

## Критерии успеха

| Метрика | Цель |
|---|---|
| BER полный | ≤ 1 % (или 0 для чистого OTA) |
| EVM | записать и сравнить с согласованной базовой линией; универсального порога прохождения не используется, пока не откалибрована нормировка амплитуды/тайминга |
| Кадр обнаружен | да |

## Живой результат 2026-06-23

Применённый к записи WAV, снятой во время OTA-прогона BPSK в стоковом shell (Lab 11.21), офлайн-демодулятор обнаружил преамбулу, восстановил 281 бит payload и измерил BER = 0 / EVM ≈ 55 %. Это проходит ворота восстановления бит, а высокий EVM остаётся диагностическим результатом, а не признаком качества модуляции. Он совпадает с базовой линией AD9361 на плате из Lab 11.14 (EVM = 54,98 %), поэтому будущие захваты следует сравнивать с этой базовой линией, пока нормировка не пересмотрена.

## Чек-лист отчёта

- [ ] Указан источник файла WAV (какая работа его записала).
- [ ] Указаны частота дискретизации WAV и центральная частота из манифеста.
- [ ] Приложен график созвездия.
- [ ] Записаны BER и EVM.
- [ ] Указано, обнаружен ли кадр и сколько бит payload восстановлено.

## Шаблон инженерного вывода

```text
Запись WAV RTL-SDR из ____ на ____ МГц (Fs = ____ МС/с) демодулирована офлайн. Обнаружение
кадра: ____. Восстановлено бит payload: ____. BER = ____. EVM = ____ %. OTA-канал BPSK
подтверждён / не подтверждён как работоспособный, потому что ______.
```
